# CLAUDE.md

## Commands

- Use `uv run` to run all Python commands (e.g. `uv run pytest`, `uv run python`)
- Run tests: `uv run pytest`
- Run single test file: `uv run pytest tests/test_foo.py`
- Run app: `uv run github-tracker`
