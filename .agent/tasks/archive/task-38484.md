# TASK-38484

**Created:** 2026-04-09

## Problem

Refactor app.py (781 lines) by extracting logically separate concerns into smaller modules. Specifically: (1) Extract the merge detection and deployment status checking logic into a new file src/github_tracker/deploy_tracker.py, (2) Extract the PR refresh logic (_do_refresh_open_prs, _refresh_focused_prs, _auto_refresh_my_prs, _auto_refresh, _load_prs_progressive) into src/github_tracker/refresh.py as standalone async functions that accept the necessary dependencies. Keep app.py as a thin Textual shell that delegates to these modules. All existing tests must pass with 100% coverage.

## Acceptance Criteria

