# TASK-46958

**Created:** 2026-04-09

## Problem

Add a PRD (production) deploy status column to the TUI, next to the existing ACC column. It should work identically to ACC - same Deployments API, same compare logic, same spinner/status display - but track a separate environment. Add a new config key 'prd_deploy_environment' (default: 'production') alongside the existing 'acc_deploy_environment'. Add PrdDeployStatus enum values (PRD_DEPLOYING, PRD_DEPLOYED, PRD_ARGO, NONE) mirroring DeployStatus. Track prd_deploy in the PullRequest model. Show the PRD column in the PR table with the same symbols as ACC. Persist prd_deploy in the state file (bump state version to 5). All existing tests must pass with 100% coverage, including new tests for the PRD column.

## Acceptance Criteria

